# معمارية مونديال 2026 (Clean Architecture)

إعادة هيكلة بنيوية فقط — **السلوك مطابق 100%** للنسخة السابقة (تأكّدنا منه ببرنامج تكافؤ يحمّل الوحدات بالترتيب ويقارن الواجهة والمخرجات).

## الهيكل الجديد

```
word2026/
├── *.html                     ← العرض فقط (بنية + روابط الوحدات؛ بلا منطق داخلي)
├── style.css
├── src/
│   ├── core/                  ← النطاق (Domain): بيانات نقية + استعلامات، بلا DOM/تخزين
│   │   ├── data.js            ← المنتخبات، الملاعب، الجدول (مصدر الحقيقة)
│   │   ├── domain.js          ← team/flagUrl/nextMatch/matchesOfGroup/strength…
│   │   └── profiles.js        ← البروفايلات + سجل المواجهات (H2H)
│   ├── infra/                 ← البنية التحتية: محوّلات المتصفّح/الإدخال-الإخراج
│   │   ├── storage.js         ← غلاف localStorage
│   │   ├── time.js            ← المنطقة الزمنية + تنسيق Intl (مع الكاش)
│   │   └── exporters.js       ← تصدير التقويم (.ics) والطباعة (PDF)
│   ├── app/                   ← التطبيق: الحالة والتنسيق
│   │   ├── clock.js           ← الساعة المركزية
│   │   └── collections.js     ← المفضّلة + الاهتمامات (مستودعات تبثّ أحداثاً)
│   ├── ui/                    ← العرض: مكوّنات DOM مشتركة
│   │   ├── toast.js  reveal.js  countdown.js
│   │   ├── match-card.js      ← مكوّن بطاقة المباراة
│   │   └── chrome.js          ← التنقّل + منتقي التوقيت + عدّاد البطل + الترحيب
│   ├── boot.js                ← جذر التركيب (يربط الطبقات عند تحميل الصفحة)
│   ├── compat.js              ← جسر التوافق الخلفي (الأسماء العالمية القديمة)
│   └── pages/                 ← متحكّم واحد لكل صفحة (home, matches, match, …)
└── data.js · main.js · squads.js  ← مُهمَلة (أصبحت ملفات تنبيه؛ يمكن حذفها)
```

## قواعد الطبقات (اتجاه الاعتماد للداخل)

```
pages ─▶ ui ─▶ app ─▶ infra ─▶ core
                 └────────────────▶ core
```

- **core** لا يعرف شيئاً عن المتصفّح (لا DOM، لا localStorage، لا Intl side-effects). دوال خالصة قابلة للاختبار.
- **infra** يغلّف واجهات المتصفّح (تخزين، وقت، تصدير) خلف واجهات بسيطة.
- **app** يدير الحالة (مفضّلة/اهتمامات/ساعة) ويبثّ أحداثاً (`favchange`/`interestchange`) فيبقى منفصلاً عن العرض.
- **ui** مكوّنات عرض مستقلّة لا تعرف ببعضها.
- **pages** متحكّمات رفيعة: تجمع المكوّنات وتربطها بالـDOM الخاص بكل صفحة.

## كيف قلّلنا الترابط؟

1. **تفكيك الوحدة العملاقة:** `main.js` (≈430 سطراً يخلط التخزين والمنطق والتصدير والـDOM) صار 13 وحدة أحادية المسؤولية.
2. **فصل البيانات عن التنسيق:** `data.js` القديم كان يخلط الأرقام بمنطق المناطق الزمنية — الآن `core/data.js` بيانات صرفة و`infra/time.js` للتنسيق.
3. **واجهة مُوحَّدة (Facade):** كل الوحدات تُركّب كائناً واحداً `window.WC` بطبقاته (`WC.storage`, `WC.favorites`, `WC.countdown`…) — واجهة مستقرّة، وداخل مفكوك.
4. **أحداث بدل استدعاءات مباشرة:** الحالة تبثّ أحداثاً تلتقطها الصفحات، فلا تعتمد طبقة الحالة على العرض.
5. **حقن الترتيب لا التضمين الصلب:** الاعتماديات تُحمَّل بترتيب واضح في الـHTML (= ترتيب الطبقات).

## التوافق الخلفي (compat.js)

متحكّمات الصفحات ما زالت تستخدم الأسماء القديمة (`WCFav`, `WCMatchCard`, `WC.displayTime`…). يربط `compat.js` هذه الأسماء بالطبقات الجديدة، فلا حاجة لتعديل أي صفحة. عند ترقية الصفحات لاحقاً لاستخدام `WC.<layer>` مباشرةً، يمكن حذف هذا الملف.

## ملاحظة عن البناء (Build)

أُبقينا على نمط `<script>` المتعدّد (بلا أداة بناء) عمداً ليعمل المشروع كما هو من نظام الملفات (`file://`) دون أي خادم — حفاظاً على السلوك. الخطوة التالية الطبيعية في بيئة إنتاج: ESM (`import/export`) أو تجميع (bundling) للوحدات في ملف واحد مُصغَّر، وهو تحوّل مباشر الآن بعد فصل المسؤوليات.

## التحقق

برنامج تكافؤ حمّل الوحدات الـ15 بالترتيب تحت بيئة متصفّح مُحاكاة وأكّد:
- اكتمال واجهة `WC` (47 عضواً) ووجود كل الأسماء القديمة (13).
- تطابق المخرجات: التوقيت (مكة 22:00 / نيويورك 15:00)، تماثل H2H، البروفايلات، وبناء بطاقة المباراة.
- نجاح فحص الصياغة لكل ملفات `src/` الـ24.
